This is a simple Python application that implements a GUI using the tkinter module to create a program 
that allows a user to input account and transaction information in order to keep a personal record of 
financial account balances. I initially wrote it as a final project for a class in college but plan to
continue working on it a bit after turning it in. 
